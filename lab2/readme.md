# file system of node js
 it allows JS code running outside the browser and interact diretly to operating system
## common operations on file/folder
- Reading and Writing files -> readFile(),writeFile(),appendFile()
- Directory management -> mkdir(),rmdir(),readdir()
- Metadata/Information -> (Data about data) -> stat(),lstat(),fstat()
- Watching for changes -> watch(),watchFile(),unwatchFile()
- Streaming large file -> createReadStream(),createWriteStream()
- File Operations -> rename(),truncate(),unlink(),link(),syslink()

# CRUD Operations
C-Create
R-Retrieve
U-Update
D-Delete

FS Module is file system communicate with Operating System

# await
It can be used with any promise in async function. That async function is also accessed by await keyword;